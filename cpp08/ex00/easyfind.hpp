#ifndef EASYFIND_HPP
#define EASYFIND_HPP

#include <algorithm>

// class error: public std::exception
// {
//     public:
//     const char* what() const throw(){
//         return "Number not found";
//     }
// };

// template <typename T>
// typename T::iterator easyfind(T& tab, int n)
// {
//     // auto it = std::find(tab.begin(), tab.end(), n);
//     typename T::iterator it = std::find(tab.begin(), tab.end(), n);

//     if (it == tab.end())
//         throw error();
//     else
//         return it;
// }









template <typename T>




































#endif